import Head from "next/head";

const AppHead: React.FC = () => {
    return (
        <Head>
            <title>Snappcloud Status Page</title>
            <meta name="author" content="Snapp" />
            <meta name="description" content="Snappcloud status page for demonstrating" />
        </Head>
    );
};

export default AppHead;
